import React, { useState } from 'react'

const GameStoreForm = ({ product: intitalProduct, notify }) => {
  const [product, setProduct] = useState(intitalProduct);
  return (
    <div>GameStoreForm</div>
  )
}

export default GameStoreForm

